/* Browser adapter for the single shared contract source. */
var TRANSFER_RULES = Object.freeze({
  WARD: window.TransboardContracts.WARD_STATUS_ACTIONS,
  EXAM: window.TransboardContracts.EXAM_STATUS_ACTIONS,
  allowedTargets: window.TransboardContracts.allowedTargets,
});
